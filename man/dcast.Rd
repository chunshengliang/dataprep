% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dcast.R
\name{dcast}
\alias{dcast}
\title{Cast a long-format data.frame into a wide format}
\usage{
dcast(
  data,
  id = NULL,
  formula = NULL,
  variable = NULL,
  value = NULL,
  value.var = NULL,
  fill = NA_real_,
  fun.aggregate = NULL,
  na.rm = FALSE,
  cores = 0L,
  verbose = FALSE
)
}
\arguments{
\item{data}{data.frame in long format.}

\item{id}{id columns (character names, integer indices,
logical mask, or NULL). Overrides formula LHS.}

\item{formula}{optional formula of the form `id1 + id2 ~ variable`.}

\item{variable}{name of the "variable" column (case/plural tolerant
when NULL).}

\item{value}{name of the "value" column (case/plural tolerant
when NULL).}

\item{value.var}{alias of `value` (reshape2 / data.table compat).}

\item{fill}{value used to fill missing cells (default NA_real_).}

\item{fun.aggregate}{optional aggregation function applied to duplicate
(id, variable) pairs before casting. If NULL, the
C++ backend uses "last write wins", which for a
canonical melt output is the right value.}

\item{na.rm}{if TRUE, skip NA/NaN values when scattering.}

\item{cores}{number of OpenMP threads; 0 = auto.}

\item{verbose}{print progress info.}
}
\value{
data.frame in wide format.
}
\description{
Thin R wrapper. All heavy lifting (key building, scatter-write, fill)
is done in C++ (`dcast_cpp`). Only `fun.aggregate` falls back to R,
because it may be an arbitrary user-supplied function.
}
